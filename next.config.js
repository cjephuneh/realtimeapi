/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // If you need to add additional configuration options, do so here
};

module.exports = nextConfig;
